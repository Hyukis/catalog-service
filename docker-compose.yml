version: "3"
services:
  db:
    container_name: catalog_service_m
    image: mysql:latest
    volumes:
      - db:/var/lib/mysql
    ports:
      - "3307:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=fhald0115@
      - MYSQL_DATABASE=catalogDB
      - MYSQL_USER=hyukis
      - MYSQL_PASSWORD=fhald0115@
      - TZ=Asia/Seoul
    restart: always

volumes:
  db:

